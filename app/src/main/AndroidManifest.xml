<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.user.computerassembly">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AdminLoginActivity" />
        <activity android:name=".AdminHomeActivity" />
        <activity android:name=".AdminRegActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".RegUserActivity" />
        <activity
            android:name=".AdminHomeDrawActivity"
            android:label="@string/title_activity_admin_home_draw"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".AddImageActivity" />
        <activity android:name=".ImagesActivity" />
        <activity android:name=".OrderProductActivity" />
        <activity android:name=".DekboActivity" />
        <activity android:name=".WebsiteActivity" />
        <activity android:name=".ConfermUserActivity" />
        <activity android:name=".MapActivity"></activity>
    </application>

</manifest>